# 2.3.1 Mượn Sách - Độc Giả - Flowchart

## Mô tả
Tính năng độc giả mượn sách từ hệ thống.

**Actor:** Độc giả  
**Phụ thuộc:** 2.1.2, 2.2.4 (Cần đăng nhập và xem chi tiết sách)

## Flowchart

```mermaid
flowchart TD
    Start([Độc giả xem chi tiết sách]) --> CheckLogin{Đã đăng nhập<br/>với vai trò độc giả?}
    CheckLogin -->|Chưa| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    CheckLogin -->|Đã đăng nhập| ClickBorrow[Click nút Mượn Sách]
    
    RedirectLogin --> End([Kết thúc])
    
    ClickBorrow --> CheckConditions{Kiểm tra điều kiện}
    
    CheckConditions --> CheckAvailable{Sách còn sẵn?<br/>số lượng > 0}
    CheckAvailable -->|Không| ErrorNoStock[Hiển thị thông báo:<br/>Sách đã hết]
    
    CheckAvailable -->|Có| CheckMaxBorrows{Độc giả đã mượn<br/>tối đa 5 cuốn?}
    CheckMaxBorrows -->|Đã đạt| ErrorMaxBorrows[Hiển thị thông báo:<br/>Bạn đã mượn tối đa<br/>số sách cho phép]
    
    CheckMaxBorrows -->|Chưa đạt| CheckPenalty{Có khoản phạt<br/>chưa thanh toán?}
    CheckPenalty -->|Có| ErrorPenalty[Hiển thị thông báo:<br/>Vui lòng thanh toán<br/>các khoản phạt trước]
    
    CheckPenalty -->|Không| SelectDuration[Chọn thời hạn mượn:<br/>Mặc định 14 ngày<br/>Tối đa 30 ngày]
    
    ErrorNoStock --> End
    ErrorMaxBorrows --> End
    ErrorPenalty --> End
    
    SelectDuration --> ValidateDuration{Thời hạn mượn<br/>hợp lệ?<br/>1-30 ngày}
    ValidateDuration -->|Không| ErrorDuration[Hiển thị lỗi:<br/>Thời hạn mượn không hợp lệ]
    ValidateDuration -->|Hợp lệ| CreateBorrowRequest[Tạo đơn mượn với:<br/>- Trạng thái: Chờ xác nhận<br/>- Thời hạn mượn: Giá trị đã chọn]
    
    ErrorDuration --> SelectDuration
    
    CreateBorrowRequest --> Success[Hiển thị thông báo:<br/>Đã tạo yêu cầu mượn sách<br/>Chờ nhân viên xác nhận]
    Success --> End
    
    style Start fill:#e1f5ff
    style End fill:#c8e6c9
    style RedirectLogin fill:#fff9c4
    style ErrorNoStock fill:#ffcdd2
    style ErrorMaxBorrows fill:#ffcdd2
    style ErrorPenalty fill:#ffcdd2
    style ErrorDuration fill:#ffcdd2
    style Success fill:#c8e6c9
```

## Điều kiện mượn sách

1. **Sách còn sẵn:** Số lượng > 0
2. **Chưa vượt quá giới hạn:** Độc giả chưa mượn quá 5 cuốn
3. **Không có phạt chưa thanh toán:** Tất cả khoản phạt đã được thanh toán

## Thời hạn mượn

- **Mặc định:** 14 ngày
- **Tối đa:** 30 ngày
- **Tối thiểu:** 1 ngày

## Luồng thay thế

1. **Sách hết:** Hiển thị thông báo "Sách đã hết"
2. **Đã mượn tối đa:** Hiển thị thông báo "Bạn đã mượn tối đa số sách cho phép"
3. **Có phạt chưa thanh toán:** Hiển thị thông báo "Vui lòng thanh toán các khoản phạt trước khi mượn sách"

## Edge Cases

- Sách không còn sẵn (số lượng = 0)
- Đã đạt giới hạn số sách mượn (5 cuốn)
- Có khoản phạt chưa thanh toán
- Thời hạn mượn vượt quá 30 ngày
- Thời hạn mượn < 1 ngày
- Chưa đăng nhập (redirect về trang đăng nhập)

