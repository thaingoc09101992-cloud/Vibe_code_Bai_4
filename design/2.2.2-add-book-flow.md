# 2.2.2 Thêm Sách Mới - Flowchart

## Mô tả
Tính năng thêm sách mới vào hệ thống.

**Actor:** Nhân viên thư viện  
**Phụ thuộc:** 2.1.2, 2.2.1 (Cần đăng nhập và có thể loại sách)

## Flowchart

```mermaid
flowchart TD
    Start([Nhân viên click Thêm Sách Mới]) --> CheckAuth{Đã đăng nhập<br/>với vai trò nhân viên?}
    CheckAuth -->|Chưa| RedirectLogin[Chuyển hướng đến<br/>trang đăng nhập]
    CheckAuth -->|Đã đăng nhập| ShowForm[Hiển thị form thêm sách]
    
    RedirectLogin --> End([Kết thúc])
    
    ShowForm --> InputData[Nhập thông tin:<br/>- Tên sách<br/>- Tác giả<br/>- Năm xuất bản<br/>- ISBN<br/>- Thể loại<br/>- Mô tả<br/>- Số lượng bản sách]
    InputData --> Validate{Validate tất cả trường}
    
    Validate -->|Tên sách trống hoặc > 100 ký tự| ErrorTitle[Hiển thị lỗi:<br/>Tên sách không hợp lệ]
    Validate -->|Tác giả trống hoặc > 100 ký tự| ErrorAuthor[Hiển thị lỗi:<br/>Tác giả không hợp lệ]
    Validate -->|Năm xuất bản < 1900 hoặc > năm hiện tại| ErrorYear[Hiển thị lỗi:<br/>Năm xuất bản không hợp lệ]
    Validate -->|ISBN không đúng định dạng<br/>ISBN-10 hoặc ISBN-13| ErrorISBN[Hiển thị lỗi:<br/>ISBN không hợp lệ]
    Validate -->|Thể loại không tồn tại| ErrorCategory[Hiển thị lỗi:<br/>Thể loại không hợp lệ]
    Validate -->|Mô tả trống hoặc > 255 ký tự| ErrorDescription[Hiển thị lỗi:<br/>Mô tả không hợp lệ]
    Validate -->|Số lượng <= 0 hoặc không phải số| ErrorQuantity[Hiển thị lỗi:<br/>Số lượng không hợp lệ]
    
    ErrorTitle --> InputData
    ErrorAuthor --> InputData
    ErrorYear --> InputData
    ErrorISBN --> InputData
    ErrorCategory --> InputData
    ErrorDescription --> InputData
    ErrorQuantity --> InputData
    
    Validate -->|Tất cả hợp lệ| CreateBook[Tạo sách với:<br/>- Trạng thái: Có sẵn<br/>- Số lượng: Giá trị đã nhập]
    CreateBook --> Success[Hiển thị thông báo:<br/>Thêm sách thành công]
    Success --> ResetForm[Reset form]
    ResetForm --> End
    
    style Start fill:#e1f5ff
    style End fill:#c8e6c9
    style RedirectLogin fill:#fff9c4
    style ErrorTitle fill:#ffcdd2
    style ErrorAuthor fill:#ffcdd2
    style ErrorYear fill:#ffcdd2
    style ErrorISBN fill:#ffcdd2
    style ErrorCategory fill:#ffcdd2
    style ErrorDescription fill:#ffcdd2
    style ErrorQuantity fill:#ffcdd2
    style Success fill:#c8e6c9
```

## Validation Rules

- **Tên sách:** Không được để trống, tối đa 100 ký tự
- **Tác giả:** Không được để trống, tối đa 100 ký tự
- **Năm xuất bản:** Năm hợp lệ (1900 - năm hiện tại)
- **ISBN:** Định dạng chuẩn ISBN-10 hoặc ISBN-13 (nếu có)
- **Thể loại:** Phải có thể loại sách (chọn từ dropdown)
- **Mô tả:** Không được để trống, tối đa 255 ký tự
- **Số lượng:** Phải > 0, kiểu số

## Luồng thay thế

1. **Validation lỗi:** Hiển thị lỗi cụ thể cho từng trường không hợp lệ
2. **Thể loại không tồn tại:** Hiển thị lỗi và yêu cầu chọn thể loại hợp lệ

## Edge Cases

- Tên sách/tác giả trống hoặc quá dài
- ISBN không đúng định dạng (ISBN-10 hoặc ISBN-13)
- Số lượng <= 0 hoặc không phải số
- Năm xuất bản không hợp lệ (1900 - năm hiện tại)
- Thể loại không tồn tại
- Mô tả trống hoặc quá dài
- Chưa có thể loại sách nào trong hệ thống

